#include<bits/stdc++.h>
using namespace std;
int main()
{
	int curupira, boitata, boto, mapinguari, iara;
	cin >> curupira >> boitata >> boto >> mapinguari >> iara;
	int total_cassava = (curupira * 300) + (boitata * 1500) + (boto * 600) + (mapinguari * 1000) + (iara * 150) + 225;
    cout << total_cassava << endl;
}

